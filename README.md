# Wharton HSDSc Hockey Phase 1b

Run end-to-end pipeline:

```bash
python -m whsdsci.run_all
```

If `python` is not available in your shell, use:

```bash
python3 -m whsdsci.run_all
```

This generates all required artifacts under `outputs/`.
